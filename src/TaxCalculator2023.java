public class TaxCalculator2023 implements TaxCalculator {
    @Override
    public float calculateTax() {
        return 1;
    }
}

//Instead of working directly with these classes, we must work with TaxCalculator Interface -> Programming to an interface